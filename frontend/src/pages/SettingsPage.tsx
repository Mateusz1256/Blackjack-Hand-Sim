export function SettingsPage() {
  return (
    <section className="panel">
      <h2>Workspace Settings</h2>
      <p className="muted">
        Backend URL, saved presets, and execution defaults will live here as the
        API surface expands.
      </p>
    </section>
  );
}
